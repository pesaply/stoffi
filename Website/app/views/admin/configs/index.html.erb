<h1>Listing admin_configs</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Pending donations limit</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_configs.each do |admin_config| %>
  <tr>
    <td><%= admin_config.name %></td>
    <td><%= admin_config.pending_donations_limit %></td>
    <td><%= link_to 'Show', admin_config %></td>
    <td><%= link_to 'Edit', edit_admin_config_path(admin_config) %></td>
    <td><%= link_to 'Destroy', admin_config, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Config', new_admin_config_path %>
