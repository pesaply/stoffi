<div class="column throw-right">
	<%= render :partial => "sidebars/user" %>
</div>

<div class="main">
	<div class="bluebox form throw-center">
		<h1 class="title"><%=t "translatee.edit" %></h1>
		<%= form_for :admin_translatee, :url => admin_translatee_path(@admin_translatee), :html => {:method => :put}  do |f| %>
			<%= render :partial => "form", :locals => { :f => f } %>
			
			<div class="bottom">
				<div class="throw-left">
					<div class="links">
						<%= link_to t("links.show"), admin_translatee_path(@admin_translatee) %>
						<%= link_to t("links.back"), admin_translatees_path %>
					</div>
				</div>
				<div class="throw-right">
					<%= submit_tag t("links.save"), :class => "button" %>
				</div>
			</div>
		<% end %>
	</div>
</div>